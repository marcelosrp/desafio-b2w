import React from "react";

const Profile = ({ profile }) => (
  <section className="profile">
    <h1 className="aside__defaultTitle">profile</h1>
    <p className="aside__defaultText">{profile}</p>
  </section>
);

export default Profile;
